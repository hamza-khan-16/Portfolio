# Portfolio Images

Add `Hamza.jpg` here — this is the hero and about photo shown in the portfolio.

The admin panel (at `/admin`) lets you update images dynamically via Supabase Storage.
When Supabase is configured, images are pulled from there automatically.

Default fallback: `/portfolio/Hamza.jpg`
